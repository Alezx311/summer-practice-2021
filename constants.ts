// Enums
export enum CHALK_COLORS {
	red = 'red',
	green = 'green',
	blue = 'blue',
	yellow = 'yellow',
	cyan = 'cyan',
}

// Types
export type TruthyValue = string | number | string[] | number[]
export type TruthyData = TruthyValue | Record<string, unknown>

